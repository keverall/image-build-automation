{
  "oneview": {
    "appliance": "oneview.example.com",
    "module_name": "HPEOneView.860",
    "use_winrm": false,
    "winrm": {
      "server": "oneview.example.com"
    },
    "credentials": {
      "username_env": "ONEVIEW_USER",
      "password_env": "ONEVIEW_PASSWORD"
    }
  },
  "notes": "HPE OneView configuration. Module name format is HPEOneView.<major><minor> (e.g., HPEOneView.860 for OneView 8.60). See docs/oneview-module-versions.md. Set use_winrm to true if executing commands via WinRM to a jump server."
}
