{
  "oneview": {
    "appliance": "oneview.example.com",
    "module_name": "HPOneView.Managed",
    "use_winrm": false,
    "winrm": {
      "server": "oneview.example.com"
    },
    "credentials": {
      "username_env": "ONEVIEW_USER",
      "password_env": "ONEVIEW_PASSWORD"
    }
  },
  "notes": "HPE OneView configuration. Uses HPOneView.Managed PowerShell module for server/scope management. Set use_winrm to true if executing commands via WinRM to a jump server."
}
