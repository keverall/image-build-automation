{
  "oneview": {
    "appliance": "oneview.example.com",
    "module_name": "HPEOneView.1000",
    "use_winrm": false,
    "winrm": {
      "server": "oneview.example.com"
    },
    "credentials": {
      "username_env": "ONEVIEW_USER",
      "password_env": "ONEVIEW_PASSWORD"
    }
  },
  "notes": "TEST/MOCK FIXTURE ONLY. This file is never read by live commands. Live OneView commands resolve everything from command parameters and environment variables (ONEVIEW_USER / ONEVIEW_PASSWORD / ONEVIEW_MODULE_NAME / MAINTENANCE_HOST); the 'appliance', 'module_name', and 'credentials' here are EXAMPLE values used by unit tests and mocks. The 'module_name' field is IGNORED by live code - the locked HPEOneView library is derived from the appliance's actual OneView version (or the ONEVIEW_MODULE_NAME override). For production, supply -OneViewHost / -OneViewUser / -OneViewPassword (or ONEVIEW_* env vars) at runtime."
}
