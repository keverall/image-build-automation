---
source:  ./scripts/list-commands.ps1
generated: 2026-09-10
auto_generated_by: scripts/Generate-PSDocs.ps1
---

# list-commands

<a id="top"></a>

## Table of Contents

- [Description](#description)
- [Examples](#examples)
  - [Example 1](#example-1)
- [Original Comment-Based Help](#original-comment-based-help)

<a id="description"></a>

## Description

Parses the public command files under src/powershell/Automation/Public and prints a bordered table of each command with its synopsis. This is the companion to `Get-CommandHelp` / the per-command `-Help` switch: it gives a quick inventory of "what commands exist", while `Get-CommandHelp <Name>` (or `<Name> -Help`) shows the detailed man-page reference for one command. Uses Write-Output with ANSI escape codes (not Write-Host) so that the PSScriptAnalyzer AvoidUsingWriteHost rule is not triggered, while still rendering colored output in a supporting terminal.

<a id="examples"></a>

## Examples

<a id="example-1"></a>

### Example 1

```powershell
pwsh -File scripts/list-commands.ps1
```

<a id="original-comment-based-help"></a>

## Original Comment-Based Help

```powershell
.SYNOPSIS
    List all main (exported) commands provided by the Automation module.

.DESCRIPTION
    Parses the public command files under src/powershell/Automation/Public and
    prints a bordered table of each command with its synopsis. This is the
    companion to `Get-CommandHelp` / the per-command `-Help` switch: it gives a
    quick inventory of "what commands exist", while `Get-CommandHelp <Name>`
    (or `<Name> -Help`) shows the detailed man-page reference for one command.

    Uses Write-Output with ANSI escape codes (not Write-Host) so that the
    PSScriptAnalyzer AvoidUsingWriteHost rule is not triggered, while still
    rendering colored output in a supporting terminal.

.EXAMPLE
    pwsh -File scripts/list-commands.ps1
```

---
*Auto-generated by `scripts/Generate-PSDocs.ps1` - do not edit manually.*
