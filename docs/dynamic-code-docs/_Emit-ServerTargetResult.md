---
source:  ./src/powershell/Automation/Public/Get-OneViewServerTarget.ps1
generated: 2026-09-02
auto_generated_by: scripts/Generate-PSDocs.ps1
---

# _Emit-ServerTargetResult

<a id="top"></a>

## Table of Contents

- [Description](#description)
- [Original Comment-Based Help](#original-comment-based-help)

<a id="description"></a>

## Description

Emits the server-target result via the shared, DRY _Publish-Result helper (consistent with every other automation command). By default it prints a clean, human-readable block and returns NOTHING on the success stream, so the operator never sees the raw 'Details { [...] }' hashtable dump in the terminal. Use -PassThru to also return the structured [hashtable] for scripts.

<a id="original-comment-based-help"></a>

## Original Comment-Based Help

```powershell
.SYNOPSIS
        Emits the server-target result via the shared, DRY _Publish-Result helper
        (consistent with every other automation command). By default it prints a
        clean, human-readable block and returns NOTHING on the success stream, so
        the operator never sees the raw 'Details { [...] }' hashtable dump in the
        terminal. Use -PassThru to also return the structured [hashtable] for scripts.
```

---
*Auto-generated by `scripts/Generate-PSDocs.ps1` - do not edit manually.*
